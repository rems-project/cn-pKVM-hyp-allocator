diff --git a/src/alloc.c b/src/alloc.c
index 4fcb302..00067f2 100644
--- a/src/alloc.c
+++ b/src/alloc.c
@@ -1478,7 +1478,6 @@ static int chunk_merge(struct chunk_hdr *chunk, struct hyp_allocator *allocator)
 /*@
 requires
         take HA_pre = Cn_hyp_allocator_focusing_on(allocator, chunk);
-        HA_pre.lseg.before != Chunk_nil {};
         let prev = match (HA_pre.lseg.before) {
             Chunk_nil {} => {
                 // dummy
